# RA8P1 裸机单核工程迁移到 FreeRTOS 双核工程计划书

## 1. 工程目标

本项目将已经完成硬件与软件验证的 RA8P1 Cortex-M85 裸机单核工程，迁移到 RA8P1 Cortex-M85 与 Cortex-M33 的 FreeRTOS 双核工程中喵。

迁移后的硬件连接、时钟、引脚、外设通道、摄像头、LCD、SDRAM、DA16200、模型和业务功能均以原裸机单核工程为唯一功能基准喵。

瑞萨 `image_classification_dualcore` 工程只用于参考双核启动、FreeRTOS 静态任务、Event Group、IPC、CPU0/CPU1 分工和代码组织方式，禁止直接替换本工程的硬件配置与业务实现喵。

目标工程不移植瑞萨参考项目中的 Deep Sleep、低功耗进入、低功耗退出、触摸唤醒和相关 LPM 控制逻辑喵。

DA16200 的 UART、AT、Wi-Fi 和 TCP 服务固定运行在 Cortex-M33/CPU1，Cortex-M85/CPU0 通过共享 SDRAM 和 IPC 向 CPU1 交付待发送数据喵。

## 2. 三个工程的职责边界

| 工程 | 实际工程目录 | 职责 |
|---|---|---|
| 已验证单核工程 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_CAM_GLCDC_AI\CameraTestProject\e2studio` | 硬件、引脚、时钟、外设、模型、业务逻辑和运行结果的唯一事实来源 |
| 目标 CPU0 工程 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\RA8P1DualCoreProject_CPU0` | Cortex-M85、摄像头、显示、AI、YOLO 后处理与 JPEG 编码 |
| 目标 CPU1 工程 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\RA8P1DualCoreProject_CPU1` | Cortex-M33、系统管理、IPC、SCI6 UART、DA16200、Wi-Fi 与 TCP 发送 |
| 瑞萨 CPU0 参考工程 | `D:\Lab\Lab_MCU\Renesas_RA\image_classification_dualcore\cm85_image_classification_ethosu` | 三任务结构、静态任务栈、Event Group、IPC 接收与副核启动方式 |
| 瑞萨 CPU1 参考工程 | `D:\Lab\Lab_MCU\Renesas_RA\image_classification_dualcore\cm33_system_management` | 单系统管理任务、IPC 发送、AGT 和中断组织方式 |

优先级规则为“单核工程的硬件与功能高于瑞萨参考工程，目标工程的 FSP 版本高于旧版生成文件，最终链接结果高于配置界面的表面数值”喵。

## 3. 禁止事项与修改规则

1. 未得到用户明确许可前，不修改任何现有 `.c`、`.h`、`configuration.xml`、`solution.xml`、`.cproject`、链接脚本或生成文件喵。
2. 不从瑞萨参考工程复制 `ra/`、`ra_gen/`、FSP 6.0.0 生成代码、引脚配置、时钟配置和外设实例配置喵。
3. 不直接编辑 `ra_gen/` 中的自动生成文件，所有 FSP 实例、任务和 RTOS 对象应优先通过 FSP Configurator 创建喵。
4. 每次只迁移一个可验证模块，上一阶段未闭环前不进入下一阶段喵。
5. 所有临时测试代码必须使用 `/*正式版删除*/` 标记，测试通过后由用户确认是否删除喵。
6. 可以为用户函数补充汉语注释，但必须先说明拟修改文件和修改范围并取得许可喵。
7. 当前顶层目录中的 `.git` 为空目录，不能提供版本回退能力，因此第一次实质修改前必须建立可恢复检查点喵。

## 4. 目标双核架构

```text
Cortex-M85 / CPU0
  ├─ Display Thread，静态栈 0x1000，优先级 3
  │    └─ GLCDC、D/AVE 2D、VSYNC、结果叠加
  ├─ Camera Thread，静态栈 0x1800，优先级 3
  │    └─ I2C、Camera Sensor、MIPI-CSI、VIN、帧就绪
  └─ AI Inference Thread，静态栈 0x2000，优先级 1
       └─ 预处理、Ethos-U55、YOLO 后处理、JPEG 编码与共享载荷发布

  线程同步：一个静态 Event Group
  副核启动：CPU0 完成基础图像与 AI 闭环后启动 CPU1
  核间通信：FSP IPC Channel 0、核间 NMI 与共享 SDRAM 描述符

Cortex-M33 / CPU1
  └─ System Management Thread，静态栈 1024 字节，优先级 1
       └─ IPC、SCI6 UART、DA16200 AT、Wi-Fi、TCP 与系统管理

共享 SDRAM
  ├─ 非缓存控制块：状态、序号、长度、CRC、置信度与错误码
  └─ JPEG 载荷区：CPU0 写入，CPU1 只读发送
```

初始移植阶段保持瑞萨参考工程的“三个 CPU0 任务加一个 CPU1 任务”分配，不额外创建网络任务喵。

JPEG 编码和检测触发判断保留在 CPU0 的 AI 业务链中，DA16200 与 TCP 发送则放入 CPU1 的 System Management Thread 中喵。

CPU1 上阻塞式 AT/TCP 操作不得持有 CPU0 的原始摄像头帧，CPU0 必须先把完整 JPEG 副本发布到共享 SDRAM 后再通知 CPU1喵。

本项目不实现参考工程的低功耗睡眠状态机，任何 IPC 消息都不得触发 CPU0 进入 Deep Sleep喵。

## 5. 迁移步骤与验收门

### 第一步：冻结基线并建立迁移清单

工作内容如下喵。

1. 确认三个工程的真实子工程目录、CPU 核、FSP 版本和工具链版本喵。
2. 记录单核工程的 FSP 外设实例、回调、IRQ、DMA/DTC、引脚、时钟和 SDRAM 配置喵。
3. 记录单核工程的源文件、入口函数、初始化顺序和主循环数据流喵。
4. 读取单核工程 `.map`，记录 Flash、SRAM、SDRAM 与 TCM 的真实占用喵。
5. 记录瑞萨参考工程的任务、优先级、栈、Event Group、IPC 与副核启动时序喵。
6. 标出只能参考的文件、需要重新通过 FSP 生成的内容和可迁移的用户源文件喵。

验收标准如下喵。

- 计划书包含三个工程的身份、内存基线和文件迁移分类喵。
- 没有修改任何现有源代码和工程配置喵。
- 后续每个文件都能回答“来自哪里、运行在哪个核、由哪个任务调用、依赖哪个 FSP 实例”喵。

当前状态为“静态分析已完成，硬件运行基线沿用用户已确认的单核工程全部通过结论”喵。

### 第二步：设计双核 Solution 内存分区与 TCM 策略

工作内容如下喵。

1. 复制瑞萨工程的内存管理方法与静态分配风格，但根据本工程实际映像重新计算数值边界喵。
2. 为 CPU0 和 CPU1 划分 Code Flash、片上 SRAM、SDRAM 和 Option Setting 所有权喵。
3. 保留 CPU0 的 ITCM/DTCM 与 CPU1 的 CTCM/STCM，不照抄瑞萨工程中长度为零的 TCM 分区喵。
4. 第一版优先把确定性高且不需要 DMA/跨核共享的代码或数据放入 TCM喵。
5. 摄像头帧、GLCDC 帧、JPEG 缓冲、NPU 需要访问的张量及 IPC 共享区不得未经验证放入 TCM喵。
6. 从物理 SDRAM 中预留一段不属于 CPU0/CPU1 普通堆、帧缓冲或模型区的共享窗口，并在两个工程中使用相同的固定基址与容量喵。
7. 为共享区定义非缓存控制块和 JPEG 载荷区，控制块至少包含 magic、协议版本、状态、序号、载荷长度、CRC、置信度和错误码喵。
8. 共享缓冲区采用 `FREE -> M85_FILLING -> READY_FOR_M33 -> M33_SENDING -> FREE/ERROR` 状态机，只有当前所有者可以推进状态喵。
9. CPU0 发布前完成 JPEG 载荷写入、D-Cache clean 和 `__DMB()`，CPU1 看到 READY 后再读取；CPU1 完成发送后使用内存屏障更新状态并通过 IPC 回执喵。
10. 生成 CPU0、CPU1 后分别检查 `memory_regions.lld` 与 `.map`，不得仅看 Solution Editor 数值喵。

建议的 TCM 验证顺序如下喵。

1. 先保留 TCM 分区但不放置业务对象，确认双核最小程序正常运行喵。
2. 再移动一个带 `/*正式版删除*/` 标记的测试函数或测试数组，确认 `.map` 地址落入目标 TCM喵。
3. 分别进行复位、冷启动、CPU0 单核运行和双核启动测试喵。
4. 最后才决定是否将任务栈、热点预处理函数或小型工作缓冲区放入 TCM喵。

验收标准为 CPU0/CPU1 均可链接、内存区不重叠、所有段均在分区内、TCM 对象地址正确且双核启动稳定喵。

### 第三步：在目标 CPU0 工程重建本机硬件配置

工作内容如下喵。

1. 以单核工程 `configuration.xml` 为对照，在目标 FSP 6.5.0 中逐项新建本机实际使用的 Stack 和 Driver喵。
2. 迁移本机时钟树、SDRAM 参数、引脚复用和电气属性喵。
3. 迁移 IIC、MIPI PHY、MIPI CSI、VIN、GLCDC、DTC、D/AVE 2D、Ethos-U55 和 `RM_ETHOSU` 等 CPU0 实例喵。
4. 保持单核工程已有实例名称或建立明确的名称映射，避免业务源文件大范围改名喵。
5. 每新增一个外设都先生成并编译，不一次性堆入全部外设喵。

此步骤禁止把瑞萨 FSP 6.0.0 的 `configuration.xml` 或 `ra_gen` 覆盖到目标 FSP 6.5.0 工程中喵。

验收标准为目标 CPU0 在尚未迁移业务代码时可无警告生成 FSP 代码并成功链接喵。

### 第四步：建立 FreeRTOS 主服务与同步对象

工作内容如下喵。

1. 在 CPU0 创建显示、摄像头和 AI 三个线程，采用当前目标工程已经确定的 snake_case Symbol 命名：`display_thread`、`camera_thread` 和 `ai_thread`；任务显示名称分别为 `Display Thread`、`Camera Thread` 和 `AI Thread`，优先级、静态分配、初始栈大小、外设 Stack 归属和同步组织风格继续仿照瑞萨参考工程喵。
2. 创建一个静态 Event Group，并复制参考工程的事件位组织方式喵。
3. 保持 `configTICK_RATE_HZ = 1000` 和禁用时间片的参考策略喵。
4. 初期保留静态任务分配，动态分配只保留 FSP/FreeRTOS 必要能力，不在业务代码中使用随意 `malloc` 喵。
5. 加入带 `/*正式版删除*/` 标记的任务心跳与栈水位测试喵。

线程命名决策记录：用户已按当前工程要求将三个线程改为 `display_thread`、`camera_thread`、`ai_thread`，对应入口函数采用 `display_thread_entry`、`camera_thread_entry`、`ai_thread_entry`，后续配置、代码迁移和文档统一使用这组名称喵。
其中显示与摄像头线程 Symbol 和瑞萨双核参考工程一致，AI 线程按本项目要求使用较短的 `ai_thread`，不采用参考工程的 `ai_inference_thread`；该差异仅属于命名约定，参考工程的“三个 CPU0 静态线程、一个静态 Event Group、优先级 3/3/1、初始栈 0x1000/0x1800/0x2000、关闭时间片和按线程归属外设 Stack”的结构仍作为本项目基准喵。

验收标准为三个任务都能运行，初始化屏障有效，任务栈水位可测且无栈溢出喵。

### 第五步：迁移 Display Thread

工作内容如下喵。

1. 迁移单核工程的 `glcdc_display` 与 `dave2D_overlay` 用户模块喵。
2. 保留本机 LCD 时序、分辨率、帧缓冲格式和引脚配置喵。
3. 将 GLCDC VSYNC 回调改为 FreeRTOS ISR 安全通知方式喵。
4. 明确当前显示缓冲区、绘制缓冲区和 GLCDC 正在扫描缓冲区的所有权喵。
5. 处理 CPU 写入、D/AVE 2D 写入和 GLCDC 读取之间的 Cache 一致性喵。

验收标准为先显示固定色块，再显示静态测试图，最后稳定双缓冲且无撕裂喵。

### 第六步：迁移 Camera Thread

工作内容如下喵。

1. 迁移本机 `i2c_control`、`camera_sensor` 与 `mipi_csi` 中的摄像头初始化和 VIN 控制代码喵。
2. 保留本机摄像头寄存器表、分辨率、像素格式、MIPI 时钟和 VIN 配置喵。
3. 将裸机忙等待改为任务等待或事件等待，中断中只发布最小状态喵。
4. 建立帧缓冲区状态机，禁止 Camera Thread 在 AI 或 Display 仍读取时覆盖同一帧喵。
5. 对 VIN DMA 写入后的缓冲区执行必要的 D-Cache invalidate喵。

验收标准为帧中断连续、缓冲区地址轮转正确、帧计数持续增长且图像 CRC 或固定像素检查稳定喵。

### 第七步：迁移 AI Inference Thread

工作内容如下喵。

1. 迁移本机模型目录、Ethos-U 初始化、输入预处理、推理调用和 YOLO 后处理喵。
2. 保持训练端与 MCU 端的尺寸、RGB 顺序、INT8 scale、zero point 和输出解码一致喵。
3. AI 任务等待 Camera Thread 发布输入帧，处理完成后发布结果事件喵。
4. 测量预处理、NPU、CPU 回退算子和后处理的独立耗时喵。
5. 确认模型 Arena、命令流和张量区位于 Ethos-U 可访问的内存中喵。

验收标准为同一输入图像在单核工程与双核 CPU0 工程中的输出张量或最终检测结果一致喵。

### 第八步：迁移 CPU0 JPEG 编码与共享 SDRAM 发布服务

工作内容如下喵。

1. 在 CPU0 迁移 `ImageUpload` 模块，并保留现有 JPEG 编码参数与检测事件去抖逻辑喵。
2. JPEG 输入帧在编码完成前必须保持只读所有权，Camera Thread 不得覆盖该帧喵。
3. CPU0 将完整 JPEG 数据写入共享 SDRAM 载荷区，不把 CPU0 私有指针直接发送给 CPU1喵。
4. CPU0 计算载荷长度与 CRC，完成 Cache clean 和内存屏障后把状态改为 `READY_FOR_M33`喵。
5. CPU0 通过 IPC 只发送短通知或描述符索引，不通过 IPC 逐字节搬运 JPEG 数据喵。
6. 在共享缓冲区未回到 `FREE` 前，CPU0 不得覆盖上一份载荷，并必须设置等待超时和丢帧统计喵。

验收标准为 CPU0 生成的 JPEG 具有正确 SOI/EOI、长度与 CRC，CPU1 可重复读取共享副本，且 CPU0 不会覆盖正在发送的载荷喵。

### 第九步：建立 CPU1 DA16200 网络服务与 IPC

工作内容如下喵。

1. 参考瑞萨 CPU1 工程创建一个静态 `System Management Thread`，但不迁移其触摸、LED、LPM、Deep Sleep 和唤醒逻辑喵。
2. 在 CPU1 的 FSP 6.5.0 工程中重新创建 SCI6 UART、IPC Channel 0 与核间 NMI，并核对 SCI6 的 CPU1 中断路由与实际引脚喵。
3. 将 `RingBuffer` 与 `DA16200` 模块迁移到 CPU1，保持 115200-8-N-1、UART 回调和本机 DA16200 引脚不变喵。
4. CPU0 在摄像头、显示、AI 与共享 SDRAM 最小闭环稳定后启动 CPU1喵。
5. 先完成递增计数器 IPC 测试，再完成固定字符串共享 SDRAM 测试，最后接入 JPEG 载荷喵。
6. CPU1 收到 IPC 后检查 magic、版本、状态、序号、长度边界与 CRC，验证通过后才允许 DA16200 发送喵。
7. CPU1 完成或放弃发送后写入完成状态与错误码，通过 IPC 回执 CPU0，并释放共享载荷区喵。
8. 阻塞式 AT、Wi-Fi 与 TCP 操作只能发生在 CPU1 任务上下文，UART ISR 只接收字节、更新最小状态并通知任务喵。

验收标准为 CPU0 单独运行不受影响，CPU1 可按预期启动，IPC 序号连续，JPEG CRC 一致，网络断开或发送超时后共享缓冲区可以恢复为 `FREE`喵。

### 第十步：双核联调、性能与可靠性收尾

工作内容如下喵。

1. 分别测量摄像头采集、预处理、NPU、后处理、显示、JPEG、TCP 和 IPC 时间喵。
2. 检查各任务栈水位、FreeRTOS Heap、SRAM、TCM、SDRAM 与 Flash 余量喵。
3. 检查所有 DMA、GLCDC、D/AVE 2D、NPU 和跨核共享缓冲区的 Cache 维护喵。
4. 加入外设超时、错误恢复、任务看门狗和有限重试喵。
5. 进行连续运行、网络断开、摄像头异常、CPU1 不启动、IPC 丢通知和共享缓冲区超时测试喵。

验收标准为长期运行无死锁、无缓冲覆盖、无栈溢出、无 Cache 旧数据且关键功能与单核工程一致喵。

## 6. 第一步静态基线结果

### 6.1 工程版本

| 工程 | Core | FSP | Toolchain | 当前状态 |
|---|---:|---:|---:|---|
| 单核工程 | CPU0 / Cortex-M85 | 6.4.0 | 21.1.1 | 裸机业务完整，用户确认软硬件通过 |
| 目标 CPU0 | CPU0 / Cortex-M85 | 6.5.0 | 21.1.1 | FreeRTOS 空任务骨架 |
| 目标 CPU1 | CPU1 / Cortex-M33 | 6.5.0 | 21.1.1 | FreeRTOS 空任务骨架 |
| 瑞萨参考 CPU0 | CPU0 / Cortex-M85 | 6.0.0 | 18.1.3 | 三任务参考架构 |
| 瑞萨参考 CPU1 | CPU1 / Cortex-M33 | 6.0.0 | 18.1.3 | 单任务系统管理参考架构 |

### 6.2 FreeRTOS 参考任务

| Core | 任务 | 静态栈 | 优先级 | 初始职责 |
|---|---|---:|---:|---|
| CPU0 | Display Thread | `0x1000` 字节 | 3 | 显示、VSYNC、结果叠加 |
| CPU0 | Camera Thread | `0x1800` 字节 | 3 | 摄像头、VIN、预处理与帧发布 |
| CPU0 | AI Inference Thread | `0x2000` 字节 | 1 | Ethos-U 推理与结果发布 |
| CPU1 | System Management Thread | `1024` 字节 | 1 | IPC、SCI6、DA16200、Wi-Fi、TCP 和系统管理 |

以上栈大小先照参考工程建立，再用 `uxTaskGetStackHighWaterMark()` 的实际结果调整，不把参考数值直接视为最终生产值喵。

### 6.3 内存证据

| 工程/分区 | SRAM | Code Flash | SDRAM | TCM |
|---|---:|---:|---:|---:|
| 单核工程已用至 | `0xED000` | `0xB7E00` | `0x616C00` | ITCM/DTCM 已分配但当前用量为 0 |
| 目标 CPU0 当前分区 | `0xEA000` | `0x80000` | `0x4000000` | ITCM `0x20000`，DTCM `0x20000` |
| 目标 CPU1 当前分区 | `0xEA000` | `0x80000` | `0x4000000` | CTCM `0x10000`，STCM `0x10000` |
| 瑞萨参考 CPU0 分区 | `0xA4000` | `0xB8000` | `0x2B5C00` | ITCM/DTCM 长度为 0 |
| 瑞萨参考 CPU1 分区 | `0x2000` | `0x8000` | 0 | CTCM/STCM 长度为 0 |

由此可见，瑞萨参考工程的“内存分配方法与静态管理方式”可以采用，但其分区数值不能原样复制喵。

单核工程的 SDRAM 已用量 `0x616C00` 大于参考 CPU0 的 `0x2B5C00`，而单核 Flash 已用量 `0xB7E00` 距参考 CPU0 分区末端只剩 `0x200`，尚未计入 FreeRTOS 增量喵。

目标 CPU0 当前 `0x80000` Flash 也不足以装入现有单核映像，因此第二步必须先重新划分双核 Flash，再迁移业务代码喵。

### 6.4 文件迁移分类

#### 从单核工程迁移并适配的用户模块

- `src/mipi_csi.*`
- `src/glcdc_display.*`
- `src/dave2D_overlay.*`
- `src/camera_sensor.*`
- `src/i2c_control.*`
- `src/yolo_postprocess.*`
- `src/user_config.h`
- `src/common_utils.h`
- `src/DA16200/`
- `src/RingBuffer/`
- `src/ImageUpload/`
- `src/model/`
- `src/SEGGER_RTT/`

`mipi_csi`、显示、AI、模型与 `ImageUpload` 迁移到 CPU0，`DA16200` 与 `RingBuffer` 迁移到 CPU1；这些文件只能在对应内核的 FSP 实例重建后再迁移喵。

#### 必须在目标 FSP 6.5.0 中重新创建的内容

- 时钟树与 SDRAM 初始化参数
- 全部 Pins 与 Pin Symbolic Name
- CPU0 的 IIC、MIPI PHY、MIPI CSI、VIN、GLCDC 与 DTC
- CPU1 的 SCI6 UART、IPC Channel 0 与核间 NMI
- D/AVE 2D、Ethos-U55、`RM_ETHOSU`
- FreeRTOS 三任务、静态 Event Group 与 IPC
- CPU0/CPU1 Solution 内存分区与双方可见的共享 SDRAM 保留区

#### 只参考而不复制的瑞萨内容

- 任务入口的组织风格
- 静态任务创建方式
- Event Group 初始化屏障
- IPC Channel 0 与核间 NMI 组织方式
- CPU0 延迟启动 CPU1 的时序
- CPU1 单系统管理任务结构

#### 禁止复制的瑞萨内容

- `ra/` 与 `ra_gen/`
- FSP 6.0.0 的生成文件和配置文件
- 瑞萨板卡的 Pins、时钟、LCD、OV5640、触摸和 LED 配置
- 瑞萨模型、预处理尺寸和显示分辨率
- 瑞萨工程的数值型内存分区
- 瑞萨工程的 LPM、Deep Sleep、触摸唤醒和低功耗外设控制代码

## 7. 当前第一步结论

第一步的只读静态分析已经完成，且没有修改任何现有代码或工程配置喵。

下一次实质操作应从第二步“Solution 内存分区、共享 SDRAM 与 TCM 策略”开始，先解决 CPU0 Flash 不足、CPU0 SRAM 余量、共享窗口和 TCM 放置策略，再打开 FSP 迁移硬件实例喵。
