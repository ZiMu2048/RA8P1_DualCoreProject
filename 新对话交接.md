# RA8P1 双核迁移工程新对话交接

## 1. 交接目的

本文用于让新对话直接从当前已验收状态继续工作，避免重新分析已经确认的内存、时钟、FreeRTOS、摄像头和显示配置喵。

交接日期更新为 2026-08-11，CPU0 的 OV5640、MIPI-CSI、VIN、FreeRTOS 帧通知、GLCDC 双缓冲和实时画面显示闭环已经完成硬件验收，下一阶段进入 AI Thread 迁移喵。

## 2. 工程路径与参考边界

| 用途 | 路径 |
|---|---|
| 目标工程根目录 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC` |
| 目标 CPU0 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\RA8P1DualCoreProject_CPU0` |
| 目标 CPU1 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\RA8P1DualCoreProject_CPU1` |
| 父 Solution | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\RA8P1DualCoreProject\solution.xml` |
| 总计划 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_BareMetal_AI_CAM_TCP_GLCDC\计划书.md` |
| 单核业务基线 | `D:\Lab\Lab_MCU\Renesas_RA\RA8P1_CAM_GLCDC_AI\CameraTestProject\e2studio` |
| 瑞萨双核参考工程 | `D:\Lab\Lab_MCU\Renesas_RA\image_classification_dualcore` |

单核工程用于核对本板实际外设参数、LCD 时序、摄像头参数和实例名称，瑞萨双核工程用于核对线程组织、Stack 归属和双核架构风格喵。

禁止把旧工程的 `configuration.xml`、`ra_gen`、链接脚本或 FSP 生成文件覆盖到目标工程喵。

FSP 配置由用户在 Smart Configurator 中手动完成，助手只进行 XML、生成文件和构建产物的非代码检查，除非用户在新对话中明确授权代码迁移或代码修改喵。

## 3. 总体进度

| 计划阶段 | 当前状态 | 说明 |
|---|---|---|
| 第一步：冻结基线与迁移清单 | 已完成 | 三个工程职责和迁移边界已确认 |
| 第二步：双核内存分区 | 已完成并验收 | Flash、SRAM、SDRAM、共享区和 TCM 已分配 |
| 第三步：重建 CPU0 硬件配置 | 当前所需部分已完成 | IIC/DTC、MIPI/VIN、GLCDC、DRW/D/AVE 2D 和现有 AI/Ethos-U 生成模块均可编译链接 |
| 第四步：FreeRTOS 主服务与同步对象 | 已建立并投入使用 | 三个静态线程和 `g_ai_app_event` 已运行，显示与摄像头初始化事件可发布 |
| 第五步：CPU0 Display Thread 迁移 | 已完成并验收 | 固定色、双缓冲、行检测事件、Camera帧复制和实时显示均已通过 |
| 第六步：CPU0 Camera Thread 迁移 | 已完成并验收 | OV5640、MIPI-CSI、VIN三缓冲、连续帧通知和错误事件均已接入 |
| 第七步：CPU0 AI Thread 迁移 | 下一阶段 | 先明确独立帧通知和AI输入缓冲区所有权，再迁移预处理与Ethos-U推理 |
| 后续 CPU1、IPC 与网络 | 未开始业务验收 | 必须等CPU0摄像头、显示和AI闭环稳定后再继续 |

因此新对话不要返回早期 FSP、摄像头或显示迁移阶段，也不要提前处理 CPU1、IPC 和网络；应直接从 AI Thread 的接口与执行流程设计开始喵。

## 4. 已确认的双核内存分区

| 区域 | 起始地址 | 大小 | 归属 |
|---|---:|---:|---|
| CPU0 Flash | `0x02000000` | `0x000D8000` | CPU0 |
| CPU1 Flash | `0x020D8000` | `0x00028000` | CPU1 |
| CPU0 SRAM | `0x22000000` | `0x00180000` | CPU0 |
| CPU1 SRAM | `0x22180000` | `0x00054000` | CPU1 |
| CPU0 SDRAM | `0x68000000` | `0x07E00000` | CPU0 |
| CPU1 SDRAM | `0x6FE00000` | `0x001E0000` | CPU1 |
| SHAREMEM | `0x6FFE0000` | `0x00020000` | 双核共享 |
| CPU0 ITCM | 芯片固定基址 | `0x00020000` | CPU0 |
| CPU0 DTCM | 芯片固定基址 | `0x00020000` | CPU0 |
| CPU1 CTCM | 芯片固定基址 | `0x00010000` | CPU1 |
| CPU1 STCM | 芯片固定基址 | `0x00010000` | CPU1 |

父 Solution 中的 SDRAM Address Shift 固定显示为 9，界面无法修改；CPU0 与 CPU1 的各自 `configuration.xml` 均为 Address Shift 8，后续以两个子工程配置为实际生成依据喵。

CPU0 的 SDRAM 初始化已启用，CPU1 的 SDRAM 初始化已禁用，避免两个内核重复初始化同一片外部 SDRAM 喵。

CPU0 D-Cache 已启用，CPU1 D-Cache 已禁用喵。

后续涉及 VIN、D/AVE 2D、GLCDC、Ethos-U55 或双核共享内存时，必须显式设计 Cache clean、invalidate、内存屏障和缓冲区所有权喵。

## 5. 已确认的 CPU0 时钟配置

当前时钟配置已与单核基线同步并通过非代码检查喵。

| 项目 | 配置 |
|---|---|
| PLL2 Source | XTAL |
| CLKOUT | HOCO |
| SCICLK | PLL2R |
| SPICLK | PLL1Q |
| CANFDCLK | PLL2R |
| GPTCLK | PLL2P |
| LCDCLK | PLL2R |

## 6. FreeRTOS 线程与同步对象

当前三个线程均采用静态分配并已成功生成喵。

| Symbol | 显示名称 | Entry | 优先级 | 栈大小 |
|---|---|---|---:|---:|
| `display_thread` | `Display Thread` | `display_thread_entry` | 3 | `0x1000` |
| `camera_thread` | `Camera Thread` | `camera_thread_entry` | 3 | `0x1800` |
| `ai_thread` | `AI Thread` | `ai_thread_entry` | 1 | `0x2000` |

AI 线程按本项目要求使用 `ai_thread`，不采用瑞萨双核参考工程中的 `ai_inference_thread` 喵。

曾经出现过 `undefined symbol: DisplayThread_entry`，原因是线程 Symbol 改名后入口函数仍保留旧名称；用户手动统一入口名称后已编译通过喵。

静态 Event Group 名称为 `g_ai_app_event` 喵。

FreeRTOS Heap 4 大小为 `0x4000`，Tick 为 1000 Hz，时间片关闭，静态分配和动态分配能力均启用，栈高水位接口和 `xTimerPendFunctionCall` 已启用喵。

动态分配能力目前只为 FSP/FreeRTOS 兼容保留，业务层不应随意使用 `malloc` 喵。

## 7. Camera Thread 已完成的 FSP Stack

当前 Stack 归属如下喵。

```text
camera_thread
├── IIC Master
│   ├── DTC TX
│   └── DTC RX
└── VIN
    └── MIPI CSI
        └── MIPI PHY
```

IIC1 实例为 `g_i2c_master_for_peripheral`，工作在 Fast Mode，SDA 为 `P511`，SCL 为 `P512`，Symbol 分别为 `SYS_I2C_SDA` 和 `SYS_I2C_SCL` 喵。

IIC 回调为 `g_i2c_master_for_peripheral_callback`，DTC TX 实例为 `g_transfer0`，DTC RX 实例为 `g_transfer1` 喵。

VIN、MIPI CSI 和 MIPI PHY 参数已与单核基线逐项比较通过，VIN 回调已经修正为 `vin_callback` 喵。

摄像头控制驱动已经迁移到 `src\Camera`，当前文件为 `camera_sensor.c/.h` 和 `i2c_control.c/.h` 喵。

驱动只保留本工程实际使用的 1024×600 路径，已经删除 `RES_XXX` 多分辨率枚举、`camera_profiles`、颜色条测试表和颜色比对数组喵。

OV5640 使用板载外部 24 MHz 有源晶振，P501 是废弃引脚，禁止为 P501 增加 XCLK GPIO 或外设输出代码喵。

OV5640 硬复位使用 P709，对应 FSP Symbol 为 `CAMERA_RESET` 喵。

`Camera Thread` 当前依次执行 `i2c_control_init()`、`camera_open()`，设置 `HARDWARE_CAMERA_INIT_DONE` 后等待 `HARDWARE_DISPLAY_INIT_DONE`，随后调用 `camera_capture_open()` 和 `camera_capture_start()` 启动连续采集喵。

`camera_capture_open()` 会清空完成帧状态，对三个 VIN 缓冲区执行必要的 D-Cache Clean/Invalidate，然后调用 `R_VIN_Open()` 喵。

`camera_capture_start()` 先调用 `R_VIN_CaptureStart()` 启动 VIN 接收，再调用 `camera_stream_on()` 启动 OV5640 连续输出；失败路径会关闭已打开的 VIN 资源喵。

`vin_callback()` 只在中断上下文发布最新完成缓冲区指针、递增帧序号并设置 `CAMERA_FRAME_READY`，错误时递增错误计数并设置 `CAMERA_CAPTURE_ERROR`，不在中断中复制图像或打印大量日志喵。

`camera_completed_frame_get()` 使用 FreeRTOS 临界区将完成缓冲区指针和对应帧序号作为一致快照返回给任务上下文喵。

当前连续采集与显示联合测试已确认以下完整成功日志喵。

```text
[CAM] Camera Thread started.
[CAM] Opening IIC master.
[CAM] Initializing OV5640.
[CAM][OV5640] Waiting for sensor power stabilization.
[CAM][OV5640] Hardware reset on P709 completed.
[CAM][OV5640] Writing sensor configuration table.
[CAM][OV5640] Configuration verified, PCLK=96000000 Hz, FPS=60.
[CAM] OV5640 initialized; waiting for Display Thread.
[CAM] Continuous capture started.
```

该日志结合 LCD 实时画面显示效果良好，证明 IIC、P709 复位、OV5640 配置、MIPI-CSI 接收、VIN 连续采集、SDRAM 帧缓冲、RTOS 帧通知以及 Camera/Display 初始化屏障已经形成完整硬件闭环喵。

连续模式接入前的单帧测试也曾得到 `frames=1`、`buffer=0x68000000`、`bytes=1228800`、`vin_errors=0` 和有效非零采样数据，为 VIN 帧完成和 SDRAM 数据有效提供了独立证据喵。

本轮曾稳定复现 `Verify reg 0x3400 failed, write=0x07, read=0x00`，最终确认不是硬件损坏，也不是上电延迟或整个 IIC/DTC 链路失效喵。

`camera_write_array()` 会对配置表逐项写入并立即回读，故障前已有数十个寄存器通过校验，实际问题是旧配置把 `0x3406[0]` 置为自动 AWB，同时又写入并严格校验 `0x3400～0x3405` 手动 RGB 增益，自动算法可能接管这些动态寄存器喵。

正式版现在使用 `AWB_AUTO_ENABLE (0U)`，配置表只写入 `AWB_MAN_CTRL`，已经删除 `0x3400～0x3405` 的固定手动增益写入，硬件重新验证通过喵。

CPU0 D-Cache 已启用，IIC TX 又由 DTC 搬运，因此 `write_reg_8bit()`、`read_reg_8bit()`、`write_reg_16bit()` 和 `read_reg_16bit()` 的全部 TX 路径均在 `R_IIC_MASTER_Write()` 前调用 `SCB_CleanDCache_by_Addr()`，保证 CPU 生成的发送缓冲区先写回 SRAM 再由 DTC 读取喵。

当前寄存器读取长度均为 1 字节，FSP IIC 驱动不会为该长度启用 RX DTC，所以没有对接收变量盲目执行 Cache invalidate 喵。

此前用于验证 MIPI/VIN 的单帧测试代码已经删除，当前运行的是正式的连续采集流程，新对话不得恢复单帧测试或重新证明已经验收的采集链路喵。

## 8. Display Thread 已完成的 FSP Stack

当前 Stack 归属和嵌套关系如下喵。

```text
display_thread
├── Display (r_glcdc)
└── DRW (r_drw)
    └── D/AVE 2D
```

GLCDC 实例为 `g_display`，回调为 `glcdc_vsync_isr`，全部模块参数及 GLCDC 引脚已与单核基线比较，差异数量为零喵。

显示输入层的关键配置如下喵。

| 项目 | 配置 |
|---|---|
| Input 0 | Enabled，1024×600，RGB565，`fb_background`，2 buffers，`.sdram_noinit` |
| Input 1 | Disabled，1024×600，ARGB4444，`fb_foreground`，2 buffers，`.sdram_noinit` |
| 输出格式 | RGB888，24-bit，RGB order，Little Endian |
| 水平时序 | Total 816，Display 800，Back Porch 8，Sync 1 |
| 垂直时序 | Total 496，Display 480，Back Porch 8，Sync 1 |
| 时钟 | Internal，Panel Clock Divisor 9 |
| TCON | TCON1=HSYNC，TCON0=VSYNC，TCON2=DE |

GLCDC 的 CLK、TCON0～2 和 24 位数据线已经按单核基线完成，Pairing 为 B，Mode 为 Custom B 喵。

附加显示 GPIO 当前状态如下喵。

| Symbol | 引脚 | 当前配置 |
|---|---|---|
| `DISP_TCON3` | `P513` | GPIO 输出，初始高电平，高驱动能力 |
| `DISP_BLEN` | `P514` | GPIO 输出，初始高电平 |
| `DISP_RESET` | `P606` | 仅设置 Symbol，GPIO 功能 Disabled |

DRW Handle 为 `d2_handle0`，中断优先级为 2，Indirect Mode 为 On，Malloc 为 Default，D/AVE 2D 正确作为 DRW 的子 Stack 喵。

`d2_handle0` 与单核显示基线一致，应继续保留；瑞萨双核参考工程中的 `d2_handle` 只作为架构参考，不应据此改名喵。

GLCDC 用户代码已经整理到 `src\Display\glcdc_display.c/.h`，其中 `init_display()` 负责初始化，`glcdc_vsync_isr()` 使用 `xEventGroupSetBitsFromISR()` 发布行检测事件，`display_camera_frame_copy()` 负责完整帧复制和 Cache 维护，`display_wait_next_vsync()` 负责在任务上下文阻塞等待新的行检测事件喵。

这里的“阻塞等待”只会把 Display Thread 置为 Blocked 状态并让出 CPU，不会忙等或阻塞整个 Cortex-M85；回调中的实际 FSP 事件是 `DISPLAY_EVENT_LINE_DETECTION`，工程事件位沿用名称 `GLCDC_VSYNC` 喵。

Display Thread 启动 GLCDC 后设置 `HARDWARE_DISPLAY_INIT_DONE`，随后等待 `CAMERA_FRAME_READY`，取得完成帧指针和序号，复制到后台缓冲区，等待行检测事件并调用 `R_GLCDC_BufferChange()`，成功后切换绘制缓冲区索引喵。

当前 GLCDC 初始扫描 `fb_background[0]`，Display Thread 首次绘制 `fb_background[1]`，换帧成功后在两个缓冲区之间交替；`FSP_ERR_INVALID_UPDATE_TIMING` 会等待下一次行检测后重试同一个后台缓冲区喵。

VIN 和 GLCDC 的行跨度均为 2048 字节，完整帧大小均为 `1024×600×2=1228800` 字节，代码已经加入编译期一致性检查；CPU 读取 VIN 帧前执行 D-Cache Invalidate，写完 GLCDC 缓冲区后执行 D-Cache Clean 和 `__DMB()` 喵。

GLCDC 输入层保持 `1024×600 RGB565`，LCD 有效输出区域为 `800×480`，当前显示采用与单核基线一致的左上角裁剪，不执行缩放喵。

已经修正的 Display 错误包括后台缓冲区索引条件恒真、等待结果错误地比较 Tick 与事件位、`R_GLCDC_Start()` 重复调用、VSYNC 超时未计数、换帧时序错误未重试以及未初始化 `.sdram_noinit` 首屏缓冲区喵。

可由调试器观察 `g_glcdc_vsync_count`、`g_glcdc_swap_ok_count`、`g_glcdc_gr1_underflow_count`、`g_glcdc_invalid_timing_count` 和 `g_glcdc_vsync_timeout_count` 喵。

当前实时摄像头画面已经由用户确认“显示效果非常好”，因此 Camera到Display 的连续图像链路正式验收通过喵。

`fb_background` 已由 FSP 生成在 `.sdram_noinit` 中，当前 MAP 和代码均可找到实体，不再使用“帧缓冲区尚未迁移”的旧结论喵。

此前红蓝填充显示测试代码已经被连续摄像头画面流程替代，不应恢复喵。

## 9. 最新构建证据

CPU0 最新一次成功构建产物位于 `RA8P1DualCoreProject_CPU0\Debug` 喵。

| 文件 | 大小 | 时间 |
|---|---:|---|
| `RA8P1DualCoreProject_CPU0.elf` | 9,471,468 bytes | 2026-08-11 16:22:09 |
| `RA8P1DualCoreProject_CPU0.srec` | 100,496 bytes | 2026-08-11 16:22:09 |
| `RA8P1DualCoreProject_CPU0.map` | 918,267 bytes | 2026-08-11 16:22:09 |

该构建已经包含自动 AWB 修正、四条 IIC TX 路径的 D-Cache Clean、连续 VIN/MIPI-CSI 采集、FreeRTOS 帧事件、GLCDC Camera帧复制与双缓冲显示、DRW/D/AVE 2D 和现有 AI/Ethos-U 生成模块并成功链接喵。

最新完整 Clean Build 的段大小为 `text=32766`、`data=670`、`bss=6185140`，没有业务代码警告和编译错误，仅存在 CMSIS-DSP 与 SEGGER RTT 第三方源码原有的 signedness 警告喵。

## 10. 已完成的清理与文档更新

此前临时 Memory Test 文件已经删除喵。

摄像头移植过程中带入的多分辨率和颜色条测试数据已经删除，当前目标工程中没有 `_Test` 或 `Test` 命名的 C/H 文件喵。

`计划书.md` 已更新为当前 snake_case 线程命名，不应再恢复旧的 `DisplayThread`、`CameraThread` 和 `AIThread` 记录喵。

无 Git 仓库时已经建立 `migration_checkpoints\20260811_before_camera_capture_formal_fix`、`migration_checkpoints\20260811_before_display_camera_integration_fix` 等恢复检查点，连续采集和显示修改前的文件均可恢复喵。

`APP_ERROR_TRAP(err)` 在当前工程中会执行断点指令并停止整个 Cortex-M85 CPU0，不只是停止 Camera Thread，因此摄像头初始化错误时 Display Thread 和屏幕也会同时停止；这不是 FreeRTOS 死锁喵。

## 11. 新对话的直接起点

新对话应先完整阅读 `计划书.md` 和本交接文件，不要重新修改已经验收的内存、时钟、IIC、OV5640、MIPI-CSI、VIN、Camera Thread、GLCDC 和 Display Thread 配置与代码喵。

用户的固定代码约束是：除测试代码外，任何正式代码未经用户明确要求一律不允许助手直接编写或修改；需要正式实现时，优先说明应在哪个文件、哪个函数和什么位置完成什么功能，由用户决定是否授权喵。

测试代码必须标记 `/*正式版删除*/`，所有新增代码注释必须仿照目标工程现有的 `[@name]`、`[@type]`、`[@usage]`、`[@argument]` 和 `[@return]` 风格喵。

未经用户明确授权不得修改 `configuration.xml`、`ra_gen`、FSP 生成文件、链接脚本或 Smart Configurator 配置喵。

后续遇到故障必须先区分已知现象、推测和已验证结论，一次只改变一个变量，并避免再次把动态摄像头寄存器的严格回读失败直接等同于硬件或总线故障喵。

下一项唯一主任务是分析并迁移 CPU0 AI Thread，首先向用户说明需要哪些函数、每个函数的输入输出、运行上下文和完整执行流程，由用户编写正式版；除非用户再次明确授权，否则助手不得直接写入正式 AI 代码喵。

推荐的最小执行顺序如下喵。

1. 只读对照单核基线的模型、预处理、Ethos-U 初始化、推理调用和 YOLO 后处理函数，列出需要迁移的源文件与函数，不复制旧版 `ra/`、`ra_gen/` 或 FSP 配置喵。
2. 先定义 Camera、Display 和 AI 三者的数据所有权，AI 不得在耗时推理期间长期持有会被 VIN 循环覆盖的原始帧指针喵。
3. 为 AI 设计独立的通知位或任务通知以及专用输入张量/帧副本，不能把 Display Thread 会清除的 `CAMERA_FRAME_READY` 简单当作两个消费者各自可靠的待处理队列喵。
4. 先完成一帧 RGB565 到模型输入尺寸、通道顺序和 INT8 量化值的预处理闭环，并与单核工程或 PC 端逐元素对比喵。
5. 再初始化 `RM_ETHOSU`、模型和 Tensor Arena，运行一次推理并分别记录预处理、NPU、CPU回退与后处理耗时喵。
6. 推理输出一致后再接入连续 AI Thread，最后才把检测框或文字结果交给 Display Thread 合成，禁止 AI Thread 与 Display Thread 同时写同一个 GLCDC 后台缓冲区喵。

AI 最小闭环稳定前不要启动 CPU1、IPC、JPEG 网络发布或 DA16200 迁移，也不要一次引入多个新抽象层喵。

## 12. 后续阶段的关键验收顺序

1. GLCDC 固定色、双缓冲与行检测事件闭环已经验收，不要重复修改喵。
2. OV5640 IIC 配置、MIPI-CSI、VIN连续采集与三缓冲闭环已经验收，不要重复修改喵。
3. Camera到Display的1024×600 RGB565实时画面闭环已经验收，不要恢复单帧或红蓝测试代码喵。
4. 下一阶段依次完成AI帧所有权、预处理逐元素对比、单次Ethos-U推理、YOLO后处理和连续AI Thread喵。
5. AI结果正确后再恢复D/AVE 2D检测框与文字叠加，并由Display Thread保持显示缓冲区的唯一写入控制喵。
6. CPU0摄像头、显示、AI、YOLO和JPEG业务闭环稳定后，再启动CPU1、IPC、共享SDRAM和DA16200网络迁移喵。

任何阶段都应一次只改变一个变量，并用生成结果、构建日志、MAP、内存窗口或硬件测量形成可观察闭环喵。
